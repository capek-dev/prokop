appId: com.prokopai.app
productName: Prokop
copyright: Copyright © 2026 Prokop

directories:
  output: release
  buildResources: build

files:
  - dist/**/*
  - dist/*.html
  - package.json

extraMetadata:
  main: dist/main.js

mac:
  category: public.app-category.productivity
  icon: build/icon.icns
  target:
    - dmg
    - zip
  hardenedRuntime: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  gatekeeperAssess: false
  type: distribution
  extendInfo:
    NSCameraUsageDescription: Prokop needs camera access for video calls
    NSMicrophoneUsageDescription: Prokop needs microphone access for audio calls
    NSUserNotificationsUsageDescription: Prokop needs notification access for alerts

win:
  target:
    - nsis
    - msi
    - portable
  icon: build/icon.ico
  artifactName: ${productName}-${version}-win.${ext}

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Prokop

msi:
  oneClick: false
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Prokop

publish:
  provider: github
  owner: capek-dev
  repo: prokop

asar: true
compression: normal

electronDownload:
  cache: .electron-cache

extraResources:
  - from: ../client/dist
    to: client/dist
    filter:
      - "**/*"
  - from: ../client/src/assets/sounds
    to: sounds
    filter:
      - "**/*.mp3"
