{
  "name": "@jean2/tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun run build.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@capekai/tool": "^1.0.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^22.19.15",
    "bun-types": "^1.3.11",
    "typescript": "^5.7.2"
  },
  "_comment": "Runtime deps are kept here for the bundling build workflow (bun run build). Per-tool package.json files are the authoritative source for source-install (Arborist reify). Shared runtime deps will be removed in Phase 6 when the release pipeline switches to source artifacts."
}
