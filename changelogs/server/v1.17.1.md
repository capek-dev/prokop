### Changed

- **Pre-experiment context assembly restored**: Restored ordinary memory and skill handling and pinned the Čapek runtime to 1.0.13. Existing memory files and conversations are preserved.

### Removed

- **JEV context-selection experiment**: Removed TypeSafe relevance scoring, experimental settings, automatic memory filtering and skill preloading, per-response context inspection, and the associated API endpoints and dependency. The experimental assembler no longer runs when selection is disabled.
